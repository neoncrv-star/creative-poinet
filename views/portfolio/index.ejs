<%- include('../partials/header') %>

<section class="portfolio">
    <div style="text-align: center; margin-bottom: 3rem;">
        <h2>معرض الأعمال</h2>
        <p>اكتشف أحدث مشاريعنا وقصص نجاح عملائنا</p>
    </div>
    <div class="projects-grid">
        <% projects.forEach(project => { %>
            <div class="project-card">
                <% if(project.image) { %>
                    <div style="height: 200px; background-color: #eee; background-image: url('<%= project.image %>'); background-size: cover; background-position: center;"></div>
                <% } else { %>
                    <div style="height: 200px; background-color: #2c3e50; display: flex; align-items: center; justify-content: center; color: #fff; font-size: 3rem;">🖼️</div>
                <% } %>
                <h3><%= project.title %></h3>
                <p><%= project.description %></p>
                <a href="/portfolio/<%= project.id %>">عرض التفاصيل</a>
            </div>
        <% }) %>
    </div>
</section>

<%- include('../partials/footer') %>
